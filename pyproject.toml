[tool.poetry]
name = "ailine"
version = "0.1.0"
description = ""
authors = ["Igor Zaton"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
mlflow = "^2.21.2"
flask = "^3.1.0"
pyyaml = "^6.0.2"
zstandard = "^0.23.0"
dvc = "^3.67.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

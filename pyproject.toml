[tool.poetry]
name = "ailine"
version = "0.1.0"
description = ""
authors = ["Igor Zaton"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
mlflow = "^2.21.2"
flask = "^3.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
